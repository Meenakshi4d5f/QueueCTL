# QueueCTL
queuectl is a simple command-line job queue system that runs background jobs, supports retries with exponential backoff, and safely moves permanently failed jobs to a Dead Letter Queue (DLQ).
